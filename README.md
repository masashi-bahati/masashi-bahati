# [:+1:](https://pages.github.com/)

```mermaid
graph TD
    A[連続運転判定-開始]-->B{"作業チェック"}
    B-->|運転|C(作業時間を運転時間へ加算)
    B-->|運転以外|D{"作業時間チェック"}
    C-->E{"累積未判定時間チェック"}
    E-->|有|F(運転時間へ加算)
    F-->F2(未判定時間を0)
    E-->|無|AC
    F2-->AC[運転時間と最大運転時間を比較し最大連続運転時間を更新]
    D-->|10分以上|G(休憩時間へ加算)
    D-->|10分未満|H{"累積未判定時間チェック"}
    G-->I{"累積未判定時間チェック"}
    I-->|有|Q(休憩時間へ加算)-->Q2(未判定時間を0)-->AA
    I-->|無|AA
    H-->|有|J(未判定時間に加算)
    H-->|無|N{"前回作業チェック"}
    N-->|運転|O(未判定時間へ加算)
    N-->|運転以外|P(未判定時間を休憩時間へ加算)-->P2(未判定時間を0)-->AA
    O-->|次の明細データを処理|A
    J-->L{"累積未判定時間チェック"}
    L-->|10分以上|M(休憩時間に加算)-->M2(未判定時間を0)-->AA{"休憩時間チェック"}
    L-->|10分未満<br>次の明細データを処理|A
    AA-->|30分以上|AB(連続運転判定リセット)-->|次の明細データを処理|A
    AA-->|30分未満|AC
    AC-->|次の明細データを処理|A

ex{{<li>未判定時間-10分未満の非運転時間}}

```
